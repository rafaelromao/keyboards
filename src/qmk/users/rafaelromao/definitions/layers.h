#pragma once

typedef enum {
    _ROMAK,
    _NUMPAD,
    _ACCENT,
    _FIXED_ACCENT,
    _MACROS,
    _LOWER,
    _RAISE,
    _FIXED_NAV,
    _NAVIGATION,
    _MEDIA,
    _LOCK,
    _MAINTENANCE
} layers_t;